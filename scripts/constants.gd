extends Object
class_name Constants

const ASCII := true
const CELL_SIZE := 16
const CELL_VECTOR := Vector2(CELL_SIZE, CELL_SIZE)
const MAP_COLUMNS := 60
const MAP_ROWS := 45
const MAP_SIZE := Vector2(MAP_COLUMNS, MAP_ROWS)
const MAP_RESOLUTION := Vector2(MAP_COLUMNS, MAP_ROWS) * CELL_VECTOR
